server:
  port: 50002
spring:
  application:
    name: config-client
  cloud:
    config:
      profile: prod
      uri: http://localhost:50001/
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10001/eureka/